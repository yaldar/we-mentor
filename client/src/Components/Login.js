// Log in with linkedin using Oauth2.
// -Create and account using linkedin
// Connect linkedin id with user id in database. (Doubble check if possible).
// If no existing account in our db - redirect to profile set-up.


import React from 'react';

const Login = () => {
  return (
      <div>

      <a href="http://localhost:4000/login">Sign in with Linkedin</a>

      </div>
  );
}

export default Login;
